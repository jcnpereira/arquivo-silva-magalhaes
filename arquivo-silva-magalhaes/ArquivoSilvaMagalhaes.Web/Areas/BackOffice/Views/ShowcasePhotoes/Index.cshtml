@model IEnumerable<TranslatedViewModel<ShowcasePhoto, ShowcasePhotoTranslation>>

@{
    ViewBag.Title = "Index";

    Layout = "~/Areas/BackOffice/Views/Shared/_IndexPageLayout.cshtml";
}
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Translation.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity.CommenterName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity.CommenterEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity.IsEmailVisible)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity.VisibleSince)
            </th>
            <th>
                @UiStrings.Actions
            </th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Translation.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entity.CommenterName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entity.CommenterEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entity.IsEmailVisible, "YesNoDisplayFor")
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entity.VisibleSince)
            </td>
            <td>
                <form class="btn-group" method="post" action="@Url.Action("Delete", new { id = item.Entity.Id })">
                    <a href="@Url.Action("Details", new { id = item.Entity.Id })" class="btn btn-default" title="@UiStrings.Details">
                        <span class="glyphicon glyphicon-info-sign"></span>
                    </a>

                    <a href="@Url.Action("Edit", new { id = item.Entity.Id })" class="btn btn-default" title="@UiStrings.Edit">
                        <span class="glyphicon glyphicon-edit"></span>
                    </a>

                    @Html.AntiForgeryToken()
                    <a href="@Url.Action("Delete", new { id = item.Entity.Id })" class="btn btn-danger" title="@UiStrings.Delete" data-afsm-show-modal="true" data-afsm-delete-warning="@ShowcasePhotoStrings.DeleteWarningMessage">
                        <span class="glyphicon glyphicon-floppy-remove"></span>
                    </a>
                </form>
            </td>
        </tr>
    }
</table>