@model IEnumerable<ImageTranslation>

@{
    ViewBag.Title = ImageStrings.EntityNamePlural;
    Layout = "~/Areas/BackOffice/Views/Shared/_IndexPageLayout.cshtml";
}

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Image.ImageCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image.ProductionDate)
            </th>
            <th>
                @UiStrings.Actions
            </th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Image.ImageCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image.ProductionDate)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ImageId }) |
                @Html.ActionLink("Details", "Details", new { id = item.ImageId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ImageId }) |
                @Html.ActionLink("Espécies", "Index", "Specimens", new { imageId = item.ImageId }, null)
            </td>
        </tr>
    }
</table>