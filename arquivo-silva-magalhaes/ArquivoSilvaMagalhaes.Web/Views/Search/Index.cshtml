@using PagedList;
@using PagedList.Mvc;

@model IEnumerable<ArquivoSilvaMagalhaes.Models.ArchiveModels.CollectionTranslation>

<h2>Pesquisa</h2>
<hr />
<p>Utilize a caixa de pesquisa para inserir os termos que deseja procurar</p>
<form method="get">
    <div>
        <div class="form-group">

            <div class="input-group">
                <span class="input-group-btn">
                    <button type="submit" class="btn btn-default">@UiStrings.Done</button>
                </span>
                <input type="search"  placeholder="@UiStrings.SearchPlaceholder" name="SearchTerm" class="form-control" />
            </div>
        </div>
    </div>
</form>

<hr style="visibility: hidden" />

<ul class="media-list">
    @foreach (var item in Model)
    {
        <li class="media">
            <div class="media-body">
                <a href="~/Documents/Index/@item.CollectionId">
                    <h4 class="media-heading">@item.Title</h4>
                </a>
                <h5>@item.Provenience</h5>
                <p>
                    @Html.DisplayFor(model => item.Description)
                </p>
            </div>
        </li>
    }
</ul>
@{
    ViewBag.Title = CollectionStrings.EntityNamePlural;
    Layout = "~/Views/Shared/_IndexPageLayout.cshtml";
}