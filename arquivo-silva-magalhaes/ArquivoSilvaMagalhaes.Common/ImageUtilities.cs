
namespace ArquivoSilvaMagalhaes.Common
{
    //public class ImageUtilities
    //{
    //    public static Image GetScaledInstance(Image original, Size newSize)
    //    {
    //        if (original.Size.Width <= newSize.Width || original.Size.Height <= newSize.Height)
    //        {
    //            return original;
    //        }

    //        // Landscape. The width will be set to the maximum value.
    //        if (original.Size.Width > original.Size.Height)
    //        {
    //            // Get the scale on which we'll size the image.
    //            double scale = Convert.ToDouble(newSize.Width) / Convert.ToDouble(original.Width);

    //            int newHeight = (int)(original.Size.Height * scale);

    //            return new Bitmap(original, newSize.Width, newHeight);
    //        }
    //        // Portrait. The height will be set to the maximum value.
    //        else
    //        {
    //            // Get the scale on which we'll size the image.
    //            double scale = Convert.ToDouble(newSize.Height) / Convert.ToDouble(original.Height);

    //            int newWidth = (int)(original.Size.Width * scale);

    //            return new Bitmap(original, newWidth, newSize.Height);
    //        }
    //    }
    //}
}